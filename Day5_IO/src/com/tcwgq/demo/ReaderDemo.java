package com.tcwgq.demo;

public class ReaderDemo {
	/**
	 * 字符流Reader介绍
	 * 用于读取字符流的抽象类。
	 */
	/**
	 * 三个读取方法：
	 * int read()：读取单个字符。 读取到末尾，返回-1。
	 * int read(char[] cbuf)：读取一个字符数组。返回实际读取的字符数，末尾返回-1。
	 * int read(char[] cbuf, int off, int len)：将字符读入数组的某一部分。返回实际读取的字符数，末尾返回-1。  
	 */

}
